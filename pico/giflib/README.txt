The Pico doesn't have giflib by default.  Unpack the source code here, e.g.
pico/giflib/giflib-5.2.1

You may need to tweak the CMakeLists.txt file for the Pico build
if the giflib version or path is different.

As of this writing, giflib can be found here:
https://sourceforge.net/projects/giflib/
